[package]
name = "dino"
version = "0.1.0"
edition = "2024"

[dependencies]
macroquad = "0.4.14"
rand = "0.8"
burn = { version = "0.16", default-features = false, features = ["ndarray"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
